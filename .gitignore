ipch
.vs
*.db
*.opendb
*.aps
*.user
.code
/install/*.exe
[bB]in
build/

# Local / personal artifacts — do not publish
profiles/p4-*.json
profiles/*-baseline*.json
profiles/*-winner*.json
docs/AGENT_HANDOFF.md
*.log
agent-tools/

# Local agent / MCP tooling (consolidation_memory — working copy only)
# Agent policy is local-only on Windows (Agents.md / AGENTS.md collide). Keep in repo via .cursor/rules only.
Agents.md
AGENTS.md
mcps/
.cursor/mcp.json
.cursor/hooks.json
.mcp.json
.cursor/rules/developer-gore.local.mdc
.grok/

# wire-local-consolidation-memory.ps1 lives under the already-ignored agent-tools/
# (the script + its outputs are strictly private and must never be committed)